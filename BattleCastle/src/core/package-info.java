package core;

/*

the double linked lists are to be utilized as stacks effectively so that tiles may be processed front to back and ONLY drawn back to front as to show the first element on top

The Tree is used in collision detection. Trees can hold multiple Entries with the same Keys (Basically a list with processing of O(log2(n)) )



*/